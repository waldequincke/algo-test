# Web framework
fastapi>=0.115.0

# ASGI server
uvicorn>=0.32.0

# Fast event loop (libuv — same engine as Node.js)
uvloop>=0.21.0

# Fast HTTP parser (C extension — replaces Python's http.server parser)
httptools>=0.6.4

# Validation / serialisation — Pydantic v2 core is written in Rust
pydantic>=2.10.0

# Fast JSON encoder/decoder — replaces stdlib json (written in C)
# Used by ORJSONResponse for response serialisation
orjson>=3.10.0

# ── Test dependencies ─────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0          # required by FastAPI's TestClient (async-capable HTTP client)
