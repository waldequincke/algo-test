# Security constraints — override via env vars TREE_MAX_DEPTH / TREE_MAX_NODES
tree.max-depth=500
tree.max-nodes=10000

# Use port 8081 to run alongside java-impl (port 8080) without conflict
quarkus.http.port=8081
